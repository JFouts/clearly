@using Clearly.Crud.WebUi.Client.Models.Dto
@using Newtonsoft.Json.Linq

@code {
    [Parameter]
    public JProperty Input { get; set; } = new JProperty("null", JValue.CreateNull());

    [Parameter]
    public FieldDefinitionDto PropertyDefinitionNode { get; set; } = new FieldDefinitionDto();
}
